.container
  %table.table
    %tr
      %th Account
      %th Balance

    - @accounts.each do |account|
      %tr
        %td= link_to account.description, account_transactions_path(account.id)
        %td= account.balance.balance.format
